{
  "name": "agentish-sandbox",
  "build": { "dockerfile": "Dockerfile" },

  "features": {
    "ghcr.io/w3cj/devcontainer-features/firewall:0": {
      "verbose": true,
      "claudeCode": true,
      "mistralApi": true,
      "githubDomains": true,
      "githubIps": true,
      "npmRegistry": true,
      "pypi": true,
      "cratesIoRegistry": true,
      "ubuntuPackages": true,
      "hosts": "opencode.ai,api.opencode.ai,my.1password.com,events.1password.com,gitlab.com,codeload.github.com,objects.githubusercontent.com"
    }
  },

  "remoteUser": "rdev",
  "containerUser": "rdev",

  "mounts": [
    "source=${localEnv:HOME}/.config/nvim,target=/home/rdev/.config/nvim,type=bind,readonly",
    "source=${localEnv:HOME}/.config/zsh,target=/home/rdev/.config/zsh,type=bind,readonly",
    "source=${localEnv:HOME}/.config/bat,target=/home/rdev/.config/bat,type=bind,readonly",
    "source=${localEnv:HOME}/.config/git,target=/home/rdev/.config/git,type=bind,readonly",
    "source=${localEnv:HOME}/.config/tmux,target=/home/rdev/.config/tmux,type=bind,readonly",

    "source=${localEnv:HOME}/.config/claude,target=/home/rdev/.config/claude,type=bind",
    "source=${localEnv:HOME}/.config/pi,target=/home/rdev/.config/pi,type=bind",

    "source=${localEnv:HOME}/.config/secrets,target=/home/rdev/.config/secrets,type=bind,readonly",

    "source=${localEnv:HOME}/code/agentish,target=/home/rdev/code/agentish,type=bind,readonly",

    "source=${localEnv:HOME}/.agents,target=/home/rdev/.agents,type=bind,readonly"
  ],

  "containerEnv": {
    "PI_CODING_AGENT_DIR": "/home/rdev/.config/pi/agent",
    "CLAUDE_CONFIG_DIR": "/home/rdev/.config/claude",
    "HOST_HOME": "${localEnv:HOME}",
    "EDITOR": "nvim",
    "PAGER": "bat",
    "BAT_THEME": "Tomorrow-Night"
  },

  "postCreateCommand": "bash /usr/local/bin/post-create.sh"
}
